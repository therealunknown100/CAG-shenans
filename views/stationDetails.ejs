<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= station.name %> - Details</title>
</head>
<body>
    <h1><%= station.name %></h1>
    <img src="<%= station.image %>" alt="<%= station.name %> Image">
    <p><%= station.description %></p>
    <p>Language: <%= station.language %></p>
    <p>Stream URL: <a href="<%= station.streamUrl %>"><%= station.streamUrl %></a></p>
    
    <% if (loggedIn) { %>
        <form action="/addStation/<%= station.id %>" method="POST">
            <button type="submit">Add to Favourites</button>
        </form>
    <% } else { %>
        <p><a href="/login">Login</a> to add to favourites</p>
    <% } %>

    <a href="/stations">Back to Stations</a>
</body>
</html>


